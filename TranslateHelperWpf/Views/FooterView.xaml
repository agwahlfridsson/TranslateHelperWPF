<UserControl x:Class="TranslateHelperWpf.Views.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TranslateHelperWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300" x:Name="footerRoot">
    <UserControl.Resources>
        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" x:Key="bgPainter">
            <GradientStop Offset="0" Color="#FFEFEFEF"></GradientStop>
            <GradientStop Offset="1" Color="#FF999999"></GradientStop>
        </LinearGradientBrush>
    </UserControl.Resources>
    <Border Background="{StaticResource bgPainter}" BorderBrush="#FF666666" BorderThickness="0,1,0,0">
        <Grid>
            <StatusBar>
                <StatusBarItem>
                    <TextBlock Text="{Binding ElementName=footerRoot, Path=Revision}" FontSize="9" ToolTip="{Binding ElementName=footerRoot, Path=History}"/>
                </StatusBarItem>          
            </StatusBar>
        </Grid>
    </Border>
</UserControl>
